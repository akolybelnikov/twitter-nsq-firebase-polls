{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": ["apps/*", "libs/*"],
  "scripts": {
      "serve:polls": "yarn workspace polls run serve",
      "lint:polls": "yarn workspace polls run lint --fix"
  }
}
